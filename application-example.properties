# Archivo de ejemplo para configuración local
# Copiar este archivo como application-local.properties y ajustar los valores

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/chaski_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=tu_password_mysql

# Stripe API Key
# Obtener de: https://dashboard.stripe.com/test/apikeys
stripe.api.key=sk_test_tu_clave_secreta_de_stripe

# Server Port
server.port=8080

